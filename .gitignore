# Dependencies and builds
node_modules/
dist/
coverage/
.target/
target/

# Local protocol runtime and generated E2E output
.hexvault/
screenshots/
test-results/
playwright-report/

# Local secrets and deployment authority
.env
.env.*
!.env.example
*.keypair.json

# Editor / OS
.DS_Store
.idea/
.vscode/
.gstack/

# Local feature plans and tickets (docs/plan/rebuild predates this and stays tracked)
docs/plan/

# One-off scratch scripts (screenshot runners, Figma export shims)
*.tmp.mjs

# Local MCP server config, stays on each machine
.mcp.json

# local operator keypair, never commit
operator.json

.vercel
